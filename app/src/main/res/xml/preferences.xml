<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference android:title="Silence Mode"
        android:defaultValue="false"
        android:summary="If this option is selected, all the game's sounds are disabled"
        android:key="silence" />

    <CheckBoxPreference android:title="Vibration"
        android:defaultValue="false"
        android:summary="If this option is selected, the phone vibrate after each move"
        android:key="vibration" />

    <ListPreference android:title="Difficulty"
        android:defaultValue="middle"
        android:summary="Number of rows/columns of the pitch and of the colors of the dots"
        android:key="difficulty"
        android:entries="@array/difficultiesArray"
        android:entryValues="@array/difficultiesValuesArray"/>

</PreferenceScreen>