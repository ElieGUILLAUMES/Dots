<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference android:title="Silence Mode"
        android:defaultValue="false"
        android:summary="If this option is selected, all the game's sounds are disabled"
        android:key="silence" />

    <CheckBoxPreference android:title="Vibration"
        android:defaultValue="false"
        android:summary="If this option is selected, the phone vibrate after each move done"
        android:key="vibration" />

    <ListPreference android:title="Color number"
        android:defaultValue="3"
        android:summary="Number of dots' color. The higher it is, the more difficult is the game"
        android:key="numberColor"
        android:entries="@array/listColorNumberDescriptionArray"
        android:entryValues="@array/listColorNumberArray"/>

    <ListPreference android:title="Pitch's Size"
        android:defaultValue="6"
        android:summary="Number of rows and columns of the pitch"
        android:key="pitchSize"
        android:entries="@array/listSizeDescriptionArray"
        android:entryValues="@array/listSizeNumberArray"/>

</PreferenceScreen>